import { BorrowerViewModel } from './borrower-view-model.model';

describe('BorrowerViewModel', () => {
  it('should create an instance', () => {
    expect(new BorrowerViewModel()).toBeTruthy();
  });
});
